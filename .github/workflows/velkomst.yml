name: Generer velkomst-melding

on:
  schedule:
    - cron: "0 6 * * *"   # Kjør hver dag kl. 06:00 norsk tid (GitHub bruker UTC, så vi må evt. justere)
  workflow_dispatch:       # Lar deg fortsatt kjøre manuelt om du vil

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Sjekk ut repo
        uses: actions/checkout@v4

      - name: Sett opp Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Installer avhengigheter
        run: npm install node-fetch dotenv

      - name: Kjør script
        run: node velkomst.js
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          VOICE_ID: ${{ secrets.VOICE_ID }}
          LAT: ${{ secrets.LAT }}
          LON: ${{ secrets.LON }}

      - name: Commit og push mp3
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add velkomst.mp3
          git commit -m "Ny velkomstmelding"
          git push
